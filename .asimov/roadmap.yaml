# Forge Roadmap
# For version history, see CHANGELOG.md
# Last updated: 2025-12-06
#
# ═══════════════════════════════════════════════════════════════════════════════
# CODING STANDARDS (ADR-007)
# ═══════════════════════════════════════════════════════════════════════════════
# Philosophy: Human-readable, beautiful, well-formatted code
#
# RFC 2119 Interpretation for RoyalBit:
#   MUST   → We follow (obviously)
#   SHOULD → We follow (best practice = we do it)
#   MAY    → We don't care (no opinion needed, zero bikeshedding)
#
# Rust recommendations followed STRICTLY:
# - File size: 1,000 lines soft limit, 1,500 hard limit
# - Tests: Colocated with code (#[cfg(test)] mod tests)
# - Coverage: 100% mandatory (ADR-004)
# - Warnings: Zero tolerance
# ═══════════════════════════════════════════════════════════════════════════════

metadata:
  current_version: "5.2.0"
  last_updated: "2025-12-06"
  philosophy: "Zero tokens. Zero emissions. $40K-$132K/year saved."

# Current Release
current:
  version: "5.2.0"
  status: released
  date: "2025-12-06"
  summary: "AST Parser - Clean Architecture"
  highlights:
    - "AST parser replaces regex (5,750 → 5,582 lines, modular)"
    - "Tokenizer → Parser → Evaluator pipeline"
    - "Chrono for date functions"
    - "Both array and scalar formulas use AST"
  stats:
    tests: 918
    warnings: 0
    functions_total: 81
    coverage: "~91%"
    commands_coverage: "95.1%"

# ═══════════════════════════════════════════════════════════════════════════════
# NEXT MILESTONE
# ═══════════════════════════════════════════════════════════════════════════════

next:
  - version: "5.3.0"
    summary: "Code Organization - Human-Readable File Sizes (ADR-007)"
    priority: HIGH
    status: pending

    rationale: |
      Per ADR-007: Rust "should" becomes "must" for this project.
      Human-readable, beautiful, well-formatted code is a core value.

      Thresholds:
        - < 500 lines: Ideal
        - 500-1,000: Acceptable
        - 1,000-1,500: Evaluate for split
        - > 1,500: Split required

    deliverables:
      - task: "Split commands.rs (2,445 lines → ~14 modules)"
        status: pending
        details: |
          cli/commands/
          ├── mod.rs         (~200 lines) - shared utilities + tests
          ├── calculate.rs   - calculate() + tests
          ├── validate.rs    - validate() + tests
          ├── audit.rs       - audit() + tests
          ├── export.rs      - export() + tests
          ├── import.rs      - import() + tests
          ├── compare.rs     - compare() + tests
          ├── variance.rs    - variance() + tests
          ├── sensitivity.rs - sensitivity() + tests
          ├── goal_seek.rs   - goal_seek() + tests
          ├── break_even.rs  - break_even() + tests
          ├── functions.rs   - functions() + tests
          ├── upgrade.rs     - upgrade() + tests
          └── watch.rs       - watch() + tests

      - task: "Split parser/mod.rs (2,059 lines → ~8 modules)"
        status: pending
        details: |
          parser/
          ├── mod.rs        (~200 lines) - entry point + dispatch
          ├── single.rs     - single document parsing + tests
          ├── multi_doc.rs  - multi-document YAML + tests
          ├── includes.rs   - file includes resolution + tests
          ├── tables.rs     - table/column parsing + tests
          ├── scalars.rs    - scalar variable parsing + tests
          ├── scenarios.rs  - scenario parsing + tests
          └── validation.rs - JSON schema validation + tests

      - task: "evaluator.rs (2,918 lines) - DEFERRED"
        status: deferred
        rationale: |
          Exception per ADR-007: Match dispatch is idiomatic Rust.
          Splitting would require complex traits/fn pointers.
          16 section comments provide adequate navigation.
          Revisit if cleaner splitting pattern emerges.

    success_criteria:
      - "No file > 1,500 lines (except evaluator.rs - documented exception)"
      - "Tests colocated with code in each new module"
      - "All 965+ tests pass"
      - "Zero warnings"

# ═══════════════════════════════════════════════════════════════════════════════
# RELEASED VERSIONS (summary only - see CHANGELOG.md for details)
# ═══════════════════════════════════════════════════════════════════════════════

released:
  - version: "5.2.0"
    date: "2025-12-06"
    summary: "AST Parser - Clean Architecture"
  - version: "5.0.0"
    date: "2025-12-04"
    summary: "BREAKING: MIT → Proprietary"
  - version: "4.4.2"
    date: "2025-12-04"
    summary: "Multi-Document YAML + Includes Export"
  - version: "4.4.1"
    date: "2025-12-04"
    summary: "Scalar Math Functions Bug Fix"
  - version: "4.4.0"
    date: "2025-12-04"
    summary: "Dynamic Array Functions + Scenario Modeling"
  - version: "4.3.0"
    date: "2025-12-04"
    summary: "File Write-Back + Strict Schema Validation"
  - version: "4.2.1"
    date: "2025-12-02"
    summary: "Bug Fixes - Real-world validation issues"
  - version: "4.2.0"
    date: "2025-12-01"
    summary: "Release Automation - Cross-platform CI/CD"

# ═══════════════════════════════════════════════════════════════════════════════
# MARKET INTELLIGENCE (Updated Dec 2025)
# ═══════════════════════════════════════════════════════════════════════════════

market:
  last_updated: "2025-12-04"

  microsoft_excel:
    copilot_function:
      launched: "Nov 2025"
      description: "=COPILOT() function in cells - AI generates content"
      limits: "100 calls/10min, 300 calls/hour"
      requires: "OneDrive/SharePoint (cloud-only)"
      weakness: "Still hallucinates - probabilistic, not deterministic"

  forge_differentiators:
    vs_copilot:
      - "Deterministic (same input = same output, always)"
      - "Local execution (no cloud dependency)"
      - "Git-friendly (YAML = version control)"
      - "No hallucinations (formulas, not predictions)"
      - "Forge-native FP&A functions (variance, break-even, scenarios)"

# ═══════════════════════════════════════════════════════════════════════════════
# BACKLOG & RESEARCH
# ═══════════════════════════════════════════════════════════════════════════════

backlog:
  - id: "test-coverage-100"
    title: "100% Test Coverage (ADR-004)"
    status: "in_progress (95.1%)"
    details: |
      Current: ~91% overall, commands.rs at 95.1%
      Remaining (~5%):
      - Edge case branches in audit(), variance(), compare() helpers
      - Some error paths in sensitivity/break_even output formatting
      - watch() excluded via ADR-006 (infinite loop)
  - "OpenAPI spec + Swagger UI for API server"
  - "Forge vs Copilot benchmark suite (marketing)"
  - "Excel compatibility matrix doc"

research:
  - id: "experiential-continuity"
    title: "Experiential Continuity Layer"
    status: research
    doc: "docs/research/EXPERIENTIAL_CONTINUITY.md"
