# Project Context - Rust
# Generated by: asimov init --type rust
# This file contains project-specific configuration.
# Behavior protocols are hardcoded in the asimov binary.

# ═══════════════════════════════════════════════════════════════════════════════
# CODING STANDARDS (ADR-007)
# ═══════════════════════════════════════════════════════════════════════════════
# Philosophy: Human-readable, beautiful, well-formatted code
#
# RFC 2119 Interpretation for RoyalBit:
#   MUST   → We follow (obviously)
#   SHOULD → We follow (best practice = we do it)
#   MAY    → We don't care (no opinion needed, zero bikeshedding)
#
# Rust recommendations followed STRICTLY:
# - File size: 1,000 lines soft limit, 1,500 hard limit
# - Tests: Colocated with code (#[cfg(test)] mod tests)
# - Coverage: 100% mandatory (ADR-004)
# - Warnings: Zero tolerance
# - Linting: clippy pedantic
#
# References:
# - https://doc.rust-lang.org/book/ch07-00-managing-growing-projects-with-packages-crates-and-modules.html
# - RFC 2119: https://www.rfc-editor.org/rfc/rfc2119
# ═══════════════════════════════════════════════════════════════════════════════

identity:
  name: "my-project"
  type: rust
  version: "0.1.0"
  tagline: "Brief project description"

quality:
  test: "cargo test"
  lint: "cargo clippy -- -D warnings"
  format: "cargo fmt --check"
  build: "cargo build --release"

files:
  source:
    - "src/main.rs - Entry point"
    - "src/lib.rs - Library root"
  config:
    - "Cargo.toml - Dependencies and metadata"
  docs:
    - "README.md - Project documentation"

patterns:
  - "Result<T, E> for errors, no panics"
  - "thiserror for custom errors"
  - "No unwrap() in library code"
  - "Use clippy lints as errors"

release:
  profile: |
    [profile.release]
    opt-level = 3
    lto = true
    codegen-units = 1
    strip = true
    panic = "abort"
  compression: "UPX --best --lzma (optional)"
